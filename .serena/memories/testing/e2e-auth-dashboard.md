E2E auth screen: siza/apps/web/e2e/auth.spec.ts — 6 tests pass without env; 1 skipped (redirect to dashboard) needs TEST_USER_EMAIL/TEST_USER_PASSWORD. Dashboard/navigation: e2e/navigation.spec.ts uses setupAuthenticatedUser in beforeEach; all 13 tests require SUPABASE_SERVICE_ROLE_KEY + NEXT_PUBLIC_SUPABASE_URL. Run auth-only: npx playwright test e2e/auth.spec.ts --project=chromium. Full auth+dashboard: set .env then npx playwright test e2e/auth.spec.ts e2e/navigation.spec.ts. Fixtures: use authenticatedPage from fixtures.ts for authenticated tests; never call helpers/auth.ts directly.