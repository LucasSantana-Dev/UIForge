import { PricingPageClient } from './pricing-client';

export const metadata = {
  title: 'Pricing - Siza',
  description: 'Choose the right plan for your AI UI generation needs',
};

export default function PricingPage() {
  return <PricingPageClient />;
}
