# Docker Compose Environment Variables
# Copy this to .env for local Docker development: cp .env.docker.example .env

# Supabase Configuration (get from https://app.supabase.com/project/_/settings/api)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key

# ⚠️ SECURITY WARNING: SERVICE_ROLE_KEY
# The SUPABASE_SERVICE_ROLE_KEY bypasses Row Level Security (RLS) and grants FULL database access.
# - NEVER expose this key to client-side code or commit it to version control
# - For BACKEND-ONLY use (server-side API routes, admin operations)
# - Rotate immediately if compromised and store securely in secrets manager or CI/CD environment
# - See: https://supabase.com/docs/guides/api/api-keys#the-servicerole-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Gemini API (get free key at https://makersuite.google.com/app/apikey)
GEMINI_API_KEY=your-gemini-api-key

# Frontend URLs
FRONTEND_URL=http://localhost:3000
FRONTEND_URL_PROD=https://uiforge.pages.dev

# Rate Limiting
RATE_LIMIT_GENERATION=10
RATE_LIMIT_WINDOW_HOURS=1
RATE_LIMIT_WEBSOCKET_MESSAGES=100

# WebSocket Configuration
WS_HEARTBEAT_INTERVAL=30000
WS_MAX_CONNECTIONS=1000
